{
  "name": "stackorange",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc && cd astro && npm run build",
    "migrate:dev": "prisma migrate dev",
    "migrate": "prisma migrate deploy",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.4.0",
    "axios": "^1.8.1",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "prisma": "^6.4.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
